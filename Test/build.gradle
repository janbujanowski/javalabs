apply plugin: 'groovy'

dependencies {
    compile project(':Beans'), project(':App')
    testCompile group: 'org.spockframework', name: 'spock-core', version: '0.7-groovy-2.0'
    testCompile group: 'org.easytesting', name: 'fest-swing', version: '1.2.1'
}
